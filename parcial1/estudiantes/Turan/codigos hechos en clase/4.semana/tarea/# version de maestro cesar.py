# version de maestro cesar
def cifrar(shift,cadena):
    pass
    res = ' '
    offset =97 # a miniscula
    for _ in cadena:
        if res= ''
        res += ' '
        valor =ord(c)
        pos = ((valor + shift) - offset) %26
            =chr(pos +offset)
        res += nuevo

if __name__ == '__main__'
    shift+int(input())
    cadena= input()
    print(cifrar(shift,cadena))
